spring:
  cloud:
    nacos:
      discovery:
        server-addr: ******:8848 # Nacos服务注册与发现
      config:
        server-addr: ******:8848 # Nacos配置中心
        file-extension: yaml


minio:
  accessKey: minio
  secretKey: minio123
  bucket: eofficeforum
  endpoint: http://*****:9000
  readPath: http://*****:9000
